{
  "name": "home-dashboard",
  "version": "0.0.1",
  "private": true,
  "description": "Self-hosted hall dashboard (DAKboard replacement)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "fastify": "^5.0.0"
  }
}
